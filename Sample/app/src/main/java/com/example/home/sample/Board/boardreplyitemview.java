package com.example.home.sample.Board;

import android.content.Context;
import android.support.annotation.Nullable;
import android.util.AttributeSet;
import android.widget.LinearLayout;

public class boardreplyitemview extends LinearLayout{
    public boardreplyitemview(Context context) {
        super(context);
    }

    public boardreplyitemview(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }
}
